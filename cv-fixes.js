(() => {
  const approvedSummary = "Guest-focused hospitality professional experienced in restaurant reception, reservations, table coordination and food-and-beverage service. Skilled in welcoming guests, responding to enquiries and supporting efficient daily operations. Dependable, service-oriented and committed to continuous professional growth.";
  const storageKey = "jenisha-personal-profile-v1";

  function readSaved(){try{return JSON.parse(localStorage.getItem(storageKey)||"{}")}catch(_){return{}}}
  function writeSaved(value){localStorage.setItem(storageKey,JSON.stringify(value))}
  function isPendingDate(value){const text=String(value||"").trim().toLowerCase();return !text||text.includes("confirm")||text.includes("tbd")||text.includes("unknown")}
  function formatRange(start,end){const a=(start||"").trim(),b=(end||"").trim();if(!a&&!b)return"Dates to confirm";if(a&&!b)return a;if(!a&&b)return b;return`${a} – ${b}`}
  function markPendingDates(){document.querySelectorAll('#experienceList .date').forEach(el=>{el.classList.toggle('pending-date',isPendingDate(el.textContent));if(el.classList.contains('pending-date'))el.setAttribute('aria-label','Employment dates are incomplete and hidden in public and print views');else el.removeAttribute('aria-label')})}

  function renderReferences(saved){const heading=[...document.querySelectorAll('.section-title')].find(el=>el.textContent.trim().toLowerCase()==='references');if(!heading)return;const section=heading.closest('.section'),refs=saved.references||[],existing=section.querySelector('.summary');if(!refs.length){if(existing)existing.textContent='Available upon request.';return}if(existing)existing.remove();section.querySelectorAll('.reference-card').forEach(el=>el.remove());refs.forEach(ref=>{const card=document.createElement('div');card.className='reference-card';card.innerHTML=`<strong>${ref.name||''}</strong><br>${ref.position||''}${ref.company?` · ${ref.company}`:''}${ref.phone?`<br>${ref.phone}`:''}${ref.email?`<br>${ref.email}`:''}`;section.appendChild(card)})}

  function injectEditor(saved){const editor=document.querySelector('.editor'),controls=editor?.querySelector('.editor-controls');if(!editor||!controls||document.getElementById('extraCvEditor'))return;const exp=saved.experience||[],refs=saved.references||[{name:'Sunil Kumar Bhusal',position:'CEO',company:'Premier Hotel School',phone:'+977 9851068269',email:'sunilbhusalemail@gmail.com'},{name:'Nigam Khanal',position:'Operation Manager',company:'Premier Hotel School',phone:'+977 9802737214',email:''}];const block=document.createElement('div');block.id='extraCvEditor';block.className='extra-editor-block';block.innerHTML=`<h3>Job dates</h3><div class="field"><label>Hostess position</label><div class="date-grid"><input id="hostessStart" aria-label="Hostess start date" placeholder="Start, e.g. Feb 2021"><input id="hostessEnd" aria-label="Hostess end date" placeholder="End, e.g. Present"></div></div><div class="field"><label>Management Trainee (OJT)</label><div class="date-grid"><input id="ojtStart" aria-label="OJT start date" placeholder="Start month/year"><input id="ojtEnd" aria-label="OJT end date" placeholder="End month/year"></div></div><h3>References</h3><div class="field"><label>Reference 1 name</label><input id="ref1Name"></div><div class="field"><label>Position / company</label><input id="ref1Role"></div><div class="field"><label>Phone</label><input id="ref1Phone"></div><div class="field"><label>Email</label><input id="ref1Email"></div><div class="field"><label>Reference 2 name</label><input id="ref2Name"></div><div class="field"><label>Position / company</label><input id="ref2Role"></div><div class="field"><label>Phone</label><input id="ref2Phone"></div><p class="save-note">Incomplete job dates stay visible here for editing, but are hidden from public view and PDF.</p>`;controls.before(block);const splitDate=value=>{if(isPendingDate(value))return['',''];const parts=String(value||'').split(/\s+[–-]\s+/);return[parts[0]||'',parts.slice(1).join(' – ')||'']};const[hostStart,hostEnd]=splitDate(exp[0]?.date||'Feb 2021 – Present'),[ojtStart,ojtEnd]=splitDate(exp[1]?.date||'');const fields={hostessStart:hostStart,hostessEnd:hostEnd,ojtStart,ojtEnd,ref1Name:refs[0]?.name||'',ref1Role:[refs[0]?.position,refs[0]?.company].filter(Boolean).join(' · '),ref1Phone:refs[0]?.phone||'',ref1Email:refs[0]?.email||'',ref2Name:refs[1]?.name||'',ref2Role:[refs[1]?.position,refs[1]?.company].filter(Boolean).join(' · '),ref2Phone:refs[1]?.phone||''};Object.entries(fields).forEach(([id,value])=>{document.getElementById(id).value=value});block.addEventListener('input',()=>{const current=readSaved();current.experience=current.experience||exp;if(current.experience[0])current.experience[0].date=formatRange(document.getElementById('hostessStart').value,document.getElementById('hostessEnd').value);if(current.experience[1])current.experience[1].date=formatRange(document.getElementById('ojtStart').value,document.getElementById('ojtEnd').value);const parseRole=value=>{const parts=value.split('·').map(v=>v.trim());return{position:parts[0]||'',company:parts.slice(1).join(' · ')||''}};const r1=parseRole(document.getElementById('ref1Role').value),r2=parseRole(document.getElementById('ref2Role').value);current.references=[{name:document.getElementById('ref1Name').value.trim(),...r1,phone:document.getElementById('ref1Phone').value.trim(),email:document.getElementById('ref1Email').value.trim()},{name:document.getElementById('ref2Name').value.trim(),...r2,phone:document.getElementById('ref2Phone').value.trim(),email:''}].filter(ref=>ref.name||ref.position||ref.phone||ref.email);writeSaved(current);const dateEls=document.querySelectorAll('#experienceList .date');if(dateEls[0])dateEls[0].textContent=current.experience[0]?.date||'';if(dateEls[1])dateEls[1].textContent=current.experience[1]?.date||'';markPendingDates();renderReferences(current)})}

  function buildPremiumLayout(){const page=document.getElementById('cvPage');if(!page||page.classList.contains('premium-layout'))return;const sidebar=page.querySelector('.sidebar'),main=page.querySelector('.main'),photo=sidebar?.querySelector('.photo-wrap'),identity=main?.querySelector('.identity');const profileSection=[...main?.querySelectorAll('.section')||[]].find(section=>section.querySelector('.section-title')?.textContent.trim().toLowerCase()==='professional profile');if(!sidebar||!main||!photo||!identity||!profileSection)return;page.classList.add('premium-layout');const hero=document.createElement('header');hero.className='cv-hero';const heroContent=document.createElement('div');heroContent.className='hero-content';profileSection.classList.add('hero-summary');heroContent.append(identity,profileSection);hero.append(photo,heroContent);const body=document.createElement('div');body.className='cv-body';body.append(sidebar,main);page.replaceChildren(hero,body)}

  function applyFixes(){const output=document.getElementById('summaryOutput'),input=document.getElementById('summaryInput');if(output)output.textContent=approvedSummary;if(input)input.value=approvedSummary;const saved=readSaved();saved.summary=approvedSummary;writeSaved(saved);const photo=document.getElementById('profilePhoto');if(photo)photo.alt='Portrait of Jenisha Adhikari';const hint=document.querySelector('label[for="photoInput"]')?.closest('.field')?.nextElementSibling;if(hint?.classList.contains('hint'))hint.textContent='Upload a passport-style portrait. The full rectangular photo area is preserved without cutting the face or shoulders.';buildPremiumLayout();injectEditor(saved);renderReferences(saved);markPendingDates();document.getElementById('toggleMode')?.addEventListener('click',()=>setTimeout(markPendingDates,0));window.addEventListener('beforeprint',markPendingDates)}

  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(applyFixes,80));else setTimeout(applyFixes,80)
})();